# GraphQL query definitions
# https://gqlgen.com/getting-started/

type Query {
  users: [User!]!
  currentUser: User!

  workoutTypes: [WorkoutType!]!

  # 合トレグループ一覧
  workoutGroups: [WorkoutGroup!]!

  # 合トレグループ詳細
  workoutGroup(id: ID!): WorkoutGroup
  
}